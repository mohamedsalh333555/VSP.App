/// Legacy configuration surface intentionally contains no business truth.
/// Central operational settings are loaded from Supabase.
class AppConfig {
  const AppConfig._();

  static const bool isRetired = true;
}
