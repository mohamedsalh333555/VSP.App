# 🗺️ خريطة شاشات تطبيق VSP الحالية (SCREENS_MAP.md)

تم استخراج هذه الخريطة بناءً على الفحص الفعلي لجميع الملفات والمسارات الموجودة في كود التطبيق.

---

## 🔐 1. قسم المصادقة والنظام (Auth & Core System)

- **`/splash` — `SplashScreen`**
  - **الحالات:**
    - `loading`: جلب فحص الجلسة والتحقق من حالة تسجيل الدخول ونوع الحساب.
    - `redirection`: التوجيه الآلي التلقائي للمستخدم حسب الحالة إلى `/welcome` أو `/onboarding` أو `/verify-email` أو `/facility-onboarding` أو `/player` أو `/owner`.
  - **الخيارات والأزرار:** شعار التطبيق المتحرك (Animated Logo)، شريط التقدم المتدرج.

- **`/welcome` — `WelcomeScreen`**
  - **الحالات:**
    - `success`: عرض الشرائح التعريفية بالتطبيق (Onboarding Slider).
    - `loading`: جلب وتسجيل الدخول عبر بروتوكولات Google / Apple.
  - **الخيارات والأزرار:** محدد اللغة (عربي / English)، زر التخطي/التالي، زر التسجيل الفوري عبر Google، زر التسجيل عبر Apple، زر إنشاء حساب بالبريد الإلكتروني، رابط تسجيل الدخول للمستخدمين المسجلين.

- **`LoginScreen`** (`lib/features/auth/screens/login_screen.dart`)
  - **الحالات:**
    - `initial`: نموذج إدخال بيانات الدخول.
    - `loading`: جلب وتأكيد الاعتمادات مع Supabase/Firebase.
    - `error`: عرض تنبيه بالخطأ (كلمة المرور غير صحيحة / البريد غير مسجل / الحساب محظور).
  - **الخيارات والأزرار:** حقل البريد الإلكتروني، حقل كلمة المرور مع زر إظهار/إخفاء النص، خيار "تذكرني"، رابط "نسيت كلمة المرور"، زر الدخول (Primary CTA)، روابط الدخول الاجتماعي.

- **`SignupScreen` / `CreateAccountScreen`** (`lib/features/auth/screens/signup_screen.dart`)
  - **الحالات:**
    - `initial`: نموذج التسجيل المباشر.
    - `loading`: جلب وتحديث إنشاء الحساب الجديد.
    - `error`: البريد الإلكتروني مستخدم بالفعل أو كلمة المرور غير متوافقة مع شروط الأمان.
    - `success`: التحويل الفوري إلى شاشة تأكيد البريد الإلكتروني OTP.
  - **الخيارات والأزرار:** محدد نوع الحساب (لاعب Player / صاحب ملعب Owner)، حقل الاسم الكامل، البريد الإلكتروني، كلمة المرور وتأكيدها، موافقة الشروط والأحكام، زر إنشاء الحساب.

- **`/onboarding` — `SocialOnboardingScreen`**
  - **الحالات:**
    - `initial`: إكمال البيانات المطلوبة للحسابات الجديدة أو المسجلة عبر التواصل الاجتماعي.
    - `loading`: حفظ بيانات الملف الشخصي ورقم الهاتف.
    - `error`: رقم الهاتف مكرر أو صيغته غير صحيحة.
  - **الخيارات والأزرار:** حقل رقم الهاتف المباشر (مطلوب لضمان المصداقية)، حقل الاسم، محدد دور الحساب (لاعب / صاحب ملعب)، زر حفظ واستكمال الإعداد.

- **`/verify-email` — `VerifyEmailScreen`**
  - **الحالات:**
    - `initial`: إدخال رمز التفعيل المكون من 6 أرقام.
    - `loading`: التحقق من صحة الرمز OTP.
    - `error`: رمز التفعيل خاطئ أو انتهت صلاحيته.
    - `success`: تفعيل الحساب والدخول التلقائي للنظام.
  - **الخيارات والأزرار:** 6 حقول إدخال رمز الـ OTP، عداد تنازلي لإعادة الإرسال (Resend Timer)، زر إعادة إرسال الرمز، زر تعديل البريد الإلكتروني.

- **`/set-new-password` — `SetNewPasswordScreen`**
  - **الحالات:**
    - `initial`: نموذج إدخال كلمة المرور الجديدة.
    - `loading`: تحديث كلمة المرور عبر السيرفر.
    - `success`: تم التحديث وتأكيده.
    - `error`: الرابط منتهي أو كلمة المرور غير متطابقة.
  - **الخيارات والأزرار:** حقل كلمة المرور الجديدة، حقل تأكيد كلمة المرور، زر الحفظ والتعيين.

- **`/` — `RootScreen`**
  - **الحالات:**
    - `initial/switching`: التوجيه الديناميكي وعرض الهيكل الرئيسي للتبويبات بناءً على دور المستخدم الحالي.
  - **الخيارات والأزرار:** شريط الملاحة السفلي الموحد `VSPBottomNavBar`.

- **`/offline` — `OfflineErrorScreen`**
  - **الحالات:**
    - `error`: عدم وجود اتصال بشبكة الإنترنت أو فشل الاستجابة من الخادم.
  - **الخيارات والأزرار:** أيقونة التنبيه، نص توضيحي، زر إعادة المحاولة (Retry CTA).

- **`/suspended` — `SuspendedAccountScreen`**
  - **الحالات:**
    - `blocked`: حساب اللاعب معلق أو محظور بقرار إداري نتيجة مخالفة الشروط أو تكرار عدم الحضور.
  - **الخيارات والأزرار:** بيان سبب التعليق، زر التواصل المباشر مع الدعم الفني عبر WhatsApp أو البريد.

---

## ⚽ 2. قسم اللاعبين (Player Section)

- **`/player` — `PlayerHomeScreen` (Tab 0)**
  - **الحالات:**
    - `loading`: جلب الملاعب، المباريات العامة، والتحديات القريبة.
    - `empty`: عدم وجود ملاعب متوفرة أو تصفية لا تطابق أي ملعب.
    - `success`: عرض التغذية الرئيسية للملاعب والتحديات.
    - `error`: خطأ في تحميل البيانات.
  - **الخيارات والأزرار:** شريط البحث السريع العلوي، أيقونة الإشعارات، شريط تصفية أنواع الملاعب (5v5, 7v7, 11v11, Padel, مغطى Indoor, نجيل طبيعي)، محدد المدينة والموقع الجغرافي، قسم الملاعب الموصى بها (Recommended Stadiums)، قسم التحديات والمباريات المتاحة (Public Matches)، زر الحجز السريع لكل ملعب.

- **`StadiumDetailsScreen`** (`lib/features/player/screens/stadium_details_screen.dart`)
  - **الحالات:**
    - `loading`: تحميل تفاصيل الملعب، الصور، الخدمات، والمواعيد المتاحة.
    - `error`: تعذر جلب بيانات الملعب.
    - `success`: عرض بيانات الملعب الكاملة.
  - **الخيارات والأزرار:** معرض صور الملعب التفاعلي، زر الإضافة للمفضلة (Bookmark)، خريطة موقع الملعب الجغرافي، قائمة خدمات الملعب (إضاءة، غرف تغيير ملابس، كافيتريا، مواقف سيارات)، قسم التقييمات وآراء اللاعبين، زر "احجز الآن" الـ CTA المثبت سفلياً.

- **`BookingTypeScreen`** (`lib/features/player/screens/booking_type_screen.dart`)
  - **الحالات:**
    - `success`: عرض خيارات أنواع الحجز المتاحة للملعب المحدد.
  - **الخيارات والأزرار:** بطاقة حجز فردي/ودي (Personal Booking)، بطاقة حجز مباراة فريق (Team Match Booking)، بطاقة حجز تحدي مفتوح (Challenge Match Booking)، زر المتابعة.

- **`BookingConfirmationScreen`** (`lib/features/player/screens/booking_confirmation_screen.dart`)
  - **الحالات:**
    - `loading`: جلب جدول مواعيد الملعب المتاحة والمحجوزة لليوم المحدد.
    - `empty`: جميع الفترات الزمنية لليوم محجوزة.
    - `success`: عرض الأوقات القابلة للحجز مع خيارات التخصيص.
  - **الخيارات والأزرار:** نافذة التقويم المباشرة (Calendar Modal) + شريط الأيام الأفقي لـ 14 يوماً، قائمة خانات الـ 30 دقيقة المتاحة، خيار الملعب خاص/عام (Private Switch)، عداد عدد اللاعبين المقترح، خيار استئجار كرة، بطاقة الفاتورة وتفاصيل العربون، زر اتصل بالملعب للاستفسار، زر تأكيد الاختيارات (CTA السفلي المصغر).

- **`PaymentGatewayScreen`** (`lib/features/player/screens/payment_gateway_screen.dart`)
  - **الحالات:**
    - `initial`: عرض وسيلة الدفع وقيمة العربون/المبلغ الإجمالي.
    - `loading`: جلب ومعالجة عملية الدفع الإلكتروني أو تسجيل الحجز النقدي.
    - `success`: نجاح العملية المالية والتوجيه لشاشة التذكرة.
    - `error`: فشل عملية الدفع أو خطأ في الاتصال بمزود الخدمة.
  - **الخيارات والأزرار:** خيارات الدفع (محفظة إلكترونية Vodafone Cash / InstaPay / Fawry، أو دفع نقدي بالمباشر)، حقل رفع إيصال الدفع/صورة التحويل، زر التأكيد النهائي والدفع.

- **`BookingSuccessScreen`** (`lib/features/player/screens/booking_success_screen.dart`)
  - **الحالات:**
    - `success`: عرض تذكرة الحجز المؤكدة بنجاح.
  - **الخيارات والأزرار:** بطاقة تفاصيل التذكرة وررمز الـ QR، زر إضافة التذكير لتقويم الجهاز (Add to Calendar)، زر مشاركة تفاصيل الحجز عبر WhatsApp، زر العودة للشاشة الرئيسية.

- **`BookingsScreen`** (`lib/features/player/screens/bookings_screen.dart` - Tab 1)
  - **الحالات:**
    - `loading`: جلب قائمة حجوزات اللاعب.
    - `empty`: لا توجد حجوزات حالية أو سابقة في الحساب.
    - `success`: عرض بطاقات الحجوزات السابقة والقادمة.
  - **الخيارات والأزرار:** تابات التصفية (قادمة Upcoming / مكتملة Completed / ملغاة Cancelled)، بطاقات الحجز تفصيلية، زر إلغاء الحجز (وفق سياسة Grace Period)، زر تفاصيل المباراة، زر تقييم الملعب.

- **`/match/:bookingId` — `MatchDetailsScreen`**
  - **الحالات:**
    - `loading`: تحميل تفاصيل التحدي وقائمة الفرق واللاعبين المنضمين.
    - `error`: الحجز غير موجود أو ملغى.
    - `success`: عرض بيانات المباراة والتشكيلة المتاحة.
  - **الخيارات والأزرار:** قائمة اللاعبين الانضمام، زر الانضمام للمباراة (Join Match CTA)، زر تشكيل الفريق، زر فتح المحادثة المباشرة للمباراة (Chat Button)، زر مشاركة التحدي.

- **`ChampionScreen` / `ChampionshipDetailsScreen` / `OfficialLeagueStandingsScreen`** (`lib/features/player/screens/champion_screen.dart` - Tab 2)
  - **الحالات:**
    - `loading`: تحميل البطولات والدوريات الرسمية المتاحة.
    - `empty`: عدم وجود بطولات نشطة حالياً.
    - `success`: عرض تفاصيل البطولات وجداول الترتيب.
  - **الخيارات والأزرار:** تابات (البطولات المتاحة / دوري VSP / جدول الترتيب)، بطاقة تفاصيل البطولة، جدول ترتيب الفرق والهدافين، زر تسجيل الفريق في البطولة.

- **`MyTeamScreen` / `TeamDashboardScreen` / `TeamProfileScreen`** (`/team/:teamId` - Tab 3)
  - **الحالات:**
    - `loading`: جلب بيانات فريق اللاعب وتشكيلته.
    - `empty`: اللاعب ليس عضواً في أي فريق حالياً.
    - `success`: عرض الملف الكامل للفريق وأعضائه وإحصائياته.
  - **الخيارات والأزرار:** زر إنشاء فريق جديد (Create Team Sheet)، حقل إضافة لاعب عبر رقم الهاتف/الـ ID، محدد مراكز اللاعبين، زر تحديث الشعار والاسم، خيار مغادرة الفريق أو حذف الفريق.

- **`ChatScreen`** (`lib/features/player/screens/chat_screen.dart`)
  - **الحالات:**
    - `loading`: تحميل سجل الرسائل والمحادثة الحية.
    - `empty`: محادثة جديدة بدون رسائل سابقة.
    - `success`: عرض وتحديث الرسائل لحظياً.
  - **الخيارات والأزرار:** حقل كتابة النص، زر إرسال الرسالة، تنبيهات انضمام الأعضاء.

- **`GlobalSearchScreen`** (`lib/features/player/screens/global_search_screen.dart`)
  - **الحالات:**
    - `initial`: حقل البحث فارغ مع اقتراحات سريعة.
    - `loading`: جلب وتصفية النتائج أثناء الكتابة.
    - `empty`: لم يتم العثور على أي نتائج مطابقة.
    - `success`: عرض نتائج البحث المباشرة.
  - **الخيارات والأزرار:** حقل البحث الرئيسي، تابات التصفية (ملاعب / فرق / مباريات)، قائمة النتائج المباشرة.

- **`NotificationsCenterScreen`** (`lib/features/player/screens/notifications_center_screen.dart`)
  - **الحالات:**
    - `loading`: تحميل الإشعارات والتنبيهات.
    - `empty`: لا توجد إشعارات جديدة.
    - `success`: عرض التنبيهات مرتبة زمنياً.
  - **الخيارات والأزرار:** زر تعليم الكل كـ مقروء، بطاقات الإشعارات التفاعلية.

- **`ProfileScreen`** (`lib/features/player/screens/profile_screen.dart` - Tab 4)
  - **الحالات:**
    - `success`: عرض الملف الشخصي للاعب وإحصائياته العامة.
  - **الخيارات والأزرار:** بطاقة إحصائيات اللاعب (المباريات، الأهداف، التقييم)، قائمة الشاشات الفرعية (تعديل الملف، المفضلات، الفرق، طرق الدفع، التنبيهات، اللغة، الدعم الفني، الشروط والخصوصية)، زر تسجيل الخروج.

- **الشاشات الفرعية للملف الشخصي (Profile Subscreens):**
  - **`EditProfileScreen`**: تعديل الاسم، الصورة، المركز المفصل، القدم المفضلة، الهاتف.
  - **`FavoritesScreen`**: عرض قائمة الملاعب المفضلة وإدارتها.
  - **`PaymentMethodsScreen`**: إدارة وسائل الدفع المحفوظة.
  - **`NotificationsScreen`**: إعدادات إشعارات التطبيق والإنذارات.
  - **`LanguageScreen`**: تغيير لغة الواجهة (عربي / إنجليزي).
  - **`HelpCenterScreen`**: مركز الدعم الفني والأسئلة الشائعة.
  - **`PrivacyPolicyScreen`**: الشروط والأحكام وسياسة الخصوصية.

---

## 🏟️ 3. قسم أصحاب الملاعب (Owner Section)

- **`/facility-onboarding` — `FacilityOnboardingScreen`**
  - **الحالات:**
    - `initial`: الشاشة التمهيدية للمالك الجديد لإضافة منشأته وملاعبه.
    - `loading`: جلب قائمة الملاعب المضافة حالياً.
    - `success`: عرض الملاعب المسجلة وإتاحة إضافة ملاعب جديدة.
  - **الخيارات والأزرار:** زر إضافة ملعب جديد (Launch Add Stadium Wizard)، قائمة الملاعب المضافة حالياً، زر الانتقال لمرحلة التوثيق أو اللوحة الرئيسية.

- **`AddStadiumWizard`** (`lib/features/owner/screens/add_stadium_wizard.dart`)
  - **الحالات:**
    - `wizard_step_1`: البيانات الأساسية (اسم الملعب، نوع النجيل، المقاس).
    - `wizard_step_2`: الموقع الجغرافي وتحديد التسعير الأساسي وسعر تأجير الكرة.
    - `wizard_step_3`: مواعيد العمل الرسمية وساعات الاستراحة (Working & Break Hours).
    - `wizard_step_4`: خيارات العربون المسبق وسياسة الإلغاء ورقم التواصل مع الملعب.
    - `wizard_step_5`: رفع صور الملعب والمرفقات والخدمات المتوفرة.
  - **الخيارات والأزرار:** حقول الإدخال المتعددة، محدد ساعات العمل، خيارات التبديل (Switches)، رفع الصور، أزرار التالي / السابق / الحفظ المباشر.

- **`/documentation` — `OwnerDocumentationWizard`**
  - **الحالات:**
    - `initial`: رفع وثائق الهوية والملكية للمنشأة.
    - `loading`: رفع الملفات للسيرفر.
    - `pending/approved/rejected`: عرض حالة مراجعة الإدارة للوثائق الحالية.
  - **الخيارات والأزرار:** زر رفع صورة بطاقة الرقم القومي/الهوية، زر رفع صورة عقد الملكية/الإيجار، زر تقديم الوثائق للمراجعة.

- **`/owner` — `OwnerMainScreen`**
  - **الحالات:**
    - `success`: الهيكل الرئيسي للوحة تحكم صاحب الملعب التنقل بين 5 تبويبات.
  - **الخيارات والأزرار:** شريط الملاحة السفلي الخاص بالمالك (`VSPBottomNavBar` للمالك).

- **`OwnerDashboardScreen`** (`lib/features/owner/screens/owner_dashboard_screen.dart` - Tab 0)
  - **الحالات:**
    - `loading`: جلب ملخص الإيرادات اليومية وحجوزات اليوم والملعب الحالي.
    - `empty`: عدم وجود حجوزات لليوم الحالي.
    - `success`: عرض بطاقات الإحصائيات المالية والحجوزات القادمة اليوم.
  - **الخيارات والأزرار:** محدد الملعب الحالي (Dropdown Selector في حال وجود أكثر من ملعب)، بطاقة إجمالي الإيرادات اليومية، بطاقة عدد الحجوزات، قائمة الحجوزات اليومية المباشرة، زر القفل السريع لساعة (Quick Lock Slot).

- **`OwnerBookingsScreen`** (`lib/features/owner/screens/owner_bookings_screen.dart` - Tab 1)
  - **الحالات:**
    - `loading`: جلب وتحديث جدول المواعيد والمستجدات.
    - `success`: عرض جدول الساعات اليومي (Timeline View).
  - **الخيارات والأزرار:** محدد التاريخ (Daily Calendar Bar)، محدد الملعب، عرض الفترات الزمنية بالساعة، خيار إضافة حجز يدوي تليفوني (Manual Phone Booking Overlay)، خيار التعديل المباشر لسعر الساعة الديناميكي (Dynamic Pricing Override)، خيار تغيير حالة الحجز (مؤكد / ملغى / عدم حضور No-Show).

- **`OwnerCupScreen` / `OwnerTournamentDashboardScreen` / `CreateTournamentWizard` / `TournamentBracketsScreen`** (`lib/features/owner/screens/owner_cup_screen.dart` - Tab 2)
  - **الحالات:**
    - `loading`: جلب البطولات المنظمة بواسطة صاحب الملعب.
    - `empty`: لا توجد بطولات مقامة حالياً.
    - `success`: عرض إدارة البطولات وشجرة المباريات (Brackets View).
  - **الخيارات والأزرار:** زر إنشاء بطولة جديدة (Create Tournament Wizard)، تحديد عدد الفرق (8, 16, 32)، رسوم الاشتراك، جوائز البطولة، شجرة التصفية والتواجه، زر تسجيل وتحديث نتائج المباريات.

- **`OwnerLedgerScreen`** (`lib/features/owner/screens/owner_ledger_screen.dart` - Tab 3)
  - **الحالات:**
    - `loading`: تحميل المحفظة المالية وسجل المعاملات.
    - `empty`: لا توجد معاملة مالية مسجلة.
    - `success`: عرض رصيد الأرباح والمستحقات المتاحة.
  - **الخيارات والأزرار:** بطاقة الرصيد الصافي القابل للسحب، سجل المعاملات المالية المفصل (نقدي / كروت / عربون)، زر طلب سحب الأرباح (Withdrawal Request).

- **`OwnerAccountManagementScreen` / `OwnerProfileScreen`** (`lib/features/owner/screens/owner_profile_screen.dart` - Tab 4)
  - **الحالات:**
    - `success`: عرض بيانات حساب المالك والمنشأة وإدارتها.
  - **الخيارات والأزرار:** قائمة إدارة الملاعب المسجلة (My Stadiums Screen)، تعديل بيانات التواصل ورقم الكاش، علامة توثيق الهوية (Identity Verified Badge)، إعدادات الإشعارات، زر تسجيل الخروج.
